{
  "name": "distribucache",
  "version": "5.2.0",
  "description": "Store-backed Automatically Populating Cache",
  "keywords": [
    "cache",
    "distributed",
    "distributed-cache"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/dowjones/distribucache.git"
  },
  "author": "nemtsov@gmail.com",
  "main": "./lib",
  "scripts": {
    "test": "sh -c 'eslint examples/ lib/ test/ && npm run test-cover && istanbul check-coverage --statements 100 --functions 100 --branches 100 --lines 100'",
    "test-cover": "istanbul cover _mocha -- -r should --recursive",
    "test-watch": "mocha -r should --recursive -w -R min -t 20"
  },
  "dependencies": {
    "async": "^1.3.0",
    "common-errors": "^0.5.1",
    "joi": "^6.5.0",
    "lodash": "^3.10.0",
    "require-directory": "^2.1.1"
  },
  "devDependencies": {
    "distribucache-memory-store": "^3.1.0",
    "distribucache-redis-store": "^3.0.0",
    "eslint": "^0.24.0",
    "istanbul": "^0.3.17",
    "mocha": "^2.2.5",
    "proxyquire": "^1.6.0",
    "should": "^7.0.1",
    "sinon": "^1.15.4"
  }
}
